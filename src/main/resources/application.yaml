server:
  port: 8080
  servlet:
    session:
      persistent: false

management:
  endpoints:
    web:
      exposure:
        include: shutdown,health,info
  endpoint:
    health:
      show-details: never

spring:
  application:
    name: jobtrackr-ai
  profiles:
    active: desktop
  datasource:
#    url: jdbc:postgresql://host.docker.internal:5432/jobtrackr-db
    url: jdbc:postgresql://localhost:5432/jobtrackr-db
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD.key}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  ai:
    openai:
      api-key: ${OPEN_API.key}
      chat:
        options:
          model: gpt-4o

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope:
              - openid
              - profile
              - email
              - https://www.googleapis.com/auth/calendar
            client-name: Google Calendar

          github:
            client-id: ${GITHUB_CLIENT_ID}
            client-secret: ${GITHUB_CLIENT_SECRET}
            scope: read:user, user:email

          facebook:
            client-id: ${FACEBOOK_CLIENT_ID}
            client-secret: ${FACEBOOK_CLIENT_SECRET}
            scope: public_profile
#            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code

          linkedin:
            client-id: ${LINKEDIN_CLIENT_ID}
            client-secret: ${LINKEDIN_CLIENT_SECRET}
            client-name: LinkedIn
            authorization-grant-type: authorization_code
            redirect-uri: http://localhost:8080/login/oauth2/code/linkedin
            scope: r_liteprofile, r_emailaddress
            client-authentication-method: client_secret_post

        provider:
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth?access_type=offline&prompt=consent
            token-uri: https://oauth2.googleapis.com/token
            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo
            user-name-attribute: sub
          linkedin:
            authorization-uri: https://www.linkedin.com/oauth/v2/authorization
            token-uri: https://www.linkedin.com/oauth/v2/accessToken
            user-info-uri: https://api.linkedin.com/v2/me
            user-name-attribute: id

          facebook:
            authorization-uri: https://www.facebook.com/v18.0/dialog/oauth
            token-uri: https://graph.facebook.com/v18.0/oauth/access_token
            user-info-uri: https://graph.facebook.com/me?fields=id,name,picture
            user-name-attribute: id
jwt:
  secret: ${ENCRYPTION_SECRET}
  expiration: ${JWT_EXPIRATION:86400000}

app:
  frontend:
    url: http://localhost:3000

aws:
  s3:
    region: us-east-2
    timeToLive: 15
    bucket: jobtracker-codemaniac-510690300440
    cacheControl: max-age=31536000, public
    uploads:
      content-type: application/pdf,application/vnd.openxmlformats-officedocument.wordprocessingml.document
  cloudfront:
    domain: d1odnd8betcuzu.cloudfront.net
    keyPairId: K2RA7N1NZK00BZ
    privateKeyPath: c:\keys\jobtrackr-cf_private.pem

resume:
  maxFileSize: 5242880

google:
  refreshTokenUrl: https://oauth2.googleapis.com/token
  calendar:
    scope: https://www.googleapis.com/auth/calendar
    app-name: JobTrackr Pro

stripe:
  secret-key: ${STRIPE_API.key}
  checkout:
    success-url: ${app.frontend.url}/account?checkout=success
    cancel-url: ${app.frontend.url}/account?checkout=cancelled
  webhook:
    secret: ${STRIPE_WEBHOOK_SECRET}

brightdata:
  base-url: https://api.brightdata.com
  scrape-url: /datasets/v3/trigger
  download-snapshot-data-url: /datasets/v3/snapshot/
  webhook-callback-url: https://circularly-capillaceous-lieselotte.ngrok-free.dev/webhooks/brightdata
  webhook-secret: ${BRIGHTDATA_WEBHOOK_SECRET}
  api-key: ${BRIGHT_DATA_API_KEY}
  indeed:
    dataset-id: gd_l4dx9j9sscpvs7no2
  linkedin:
    dataset-id: gd_l1viktl72bvl7bjuj0
  glassdoor:
    dataset-id: gd_l7j0bx501ockwldaqf

billing:
  product-name: Jobtrackr


